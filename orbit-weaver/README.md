# Orbit Weaver

A self-contained interactive canvas artwork. Particles drift, form transient connections, and respond to the pointer.

Open `index.html` in a modern browser. No install step or local server is required.

Controls include particle density, motion energy, three color palettes, pause/resume, recomposition, and PNG export.
